<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <title>{{title}} - WebProj Reports</title>
  <style>
    body { 
      font-family: "Arial", sans-serif; 
      margin: 40px; 
      color: #000; 
      font-size: 12px; 
      line-height: 1.4;
    }
    h1, h2, h3 { 
      color: #000; 
      margin-bottom: 8px; 
      font-weight: bold; 
    }
    .header { 
      text-align: center; 
      margin-bottom: 40px; 
      border-bottom: 2px solid #0ea5e9; 
      padding-bottom: 15px; 
    }
    .header h1 { 
      font-size: 24px; 
      margin: 0; 
      color: #0ea5e9;
    }
    .header p { 
      margin: 4px 0; 
      color: #444; 
    }
    .report-info {
      background: #f8fafc;
      padding: 15px;
      border-radius: 6px;
      margin-bottom: 25px;
      border-left: 4px solid #0ea5e9;
    }
    table { 
      width: 100%; 
      border-collapse: collapse; 
      margin-top: 15px; 
      margin-bottom: 20px;
    }
    th, td { 
      border: 1px solid #ddd; 
      padding: 10px 12px; 
      text-align: left; 
    }
    th { 
      background-color: #0ea5e9; 
      color: white;
      font-weight: bold; 
      font-size: 11px;
      text-transform: uppercase;
      letter-spacing: 0.5px;
    }
    tr:nth-child(even) { 
      background-color: #f9f9f9; 
    }
    tr:hover {
      background-color: #f0f9ff;
    }
    .summary-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
      gap: 15px;
      margin: 15px 0;
    }
    .summary-item {
      background: #f8fafc;
      padding: 15px;
      border-radius: 6px;
      border: 1px solid #e2e8f0;
      text-align: center;
    }
    .summary-label {
      font-size: 11px;
      color: #64748b;
      text-transform: uppercase;
      letter-spacing: 0.5px;
      margin-bottom: 5px;
    }
    .summary-value {
      font-size: 18px;
      font-weight: bold;
      color: #0ea5e9;
    }
    .section {
      margin-bottom: 30px;
      page-break-inside: avoid;
    }
    .section h3 {
      color: #0ea5e9;
      border-bottom: 1px solid #e2e8f0;
      padding-bottom: 8px;
      margin-bottom: 15px;
    }
    .footer { 
      text-align: center; 
      margin-top: 50px; 
      font-size: 11px; 
      color: #64748b; 
      border-top: 1px solid #e2e8f0; 
      padding-top: 15px; 
    }
    .logo {
      font-size: 16px;
      font-weight: bold;
      color: #0ea5e9;
      margin-bottom: 5px;
    }
    .currency {
      font-weight: bold;
      color: #059669;
    }
    .percentage {
      font-weight: bold;
      color: #0ea5e9;
    }
    .status-confirmed { color: #059669; }
    .status-pending { color: #d97706; }
    .status-cancelled { color: #dc2626; }
    .status-checked-in { color: #0ea5e9; }
    .status-checked-out { color: #6b7280; }
  </style>
</head>
<body>
  <div class="header">
    <div class="logo">WebProj Hotel Management</div>
    <h1>{{title}}</h1>
    <p><strong>Report Period:</strong> {{formatDate startDate}} - {{formatDate endDate}}</p>
    <p><strong>Generated:</strong> {{formatDateTime generatedAt}}</p>
  </div>

  <div class="report-info">
    <p><strong>Report Type:</strong> {{reportType}}</p>
    <p><strong>Generated By:</strong> {{generatedBy}}</p>
    <p><strong>Data Range:</strong> {{dateRange}} days</p>
  </div>

  {{{content}}}

  <div class="footer">
    <p>Generated by WebProj Hotel Management System</p>
    <p>This report contains confidential business information</p>
  </div>
</body>
</html>
